# TODO - Build Android APK locally (Capacitor)

- [ ] Understand current Capacitor setup (capacitor.config.json, package.json scripts).
- [ ] Create/verify Android platform in Capacitor (`npx cap add android`) if missing.
- [ ] Build web assets (`npm run build`).
- [ ] Sync Capacitor project (`npx cap sync android`).
- [ ] Build debug APK using Gradle (`assembleDebug`).
- [ ] Verify output APK path (android/app/build/outputs/apk/... ) and ensure it installs.

